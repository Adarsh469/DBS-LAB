SQL> CREATE TABLE BRANCH (
  2      BRANCH_NAME VARCHAR(15) PRIMARY KEY,
  3      BRANCH_CITY VARCHAR(20),
  4      ASSETS NUMBER(10)
  5  );

Table created.

SQL>
SQL> CREATE TABLE ACCOUNT (
  2      ACCOUNT_NUMBER NUMBER(10) PRIMARY KEY,
  3      BRANCH_NAME VARCHAR(15),
  4      BALANCE NUMBER(8),
  5      FOREIGN KEY (BRANCH_NAME) REFERENCES BRANCH(BRANCH_NAME)
  6  );

Table created.

SQL>
SQL> CREATE TABLE CUSTOMER (
  2      CUSTOMER_NAME VARCHAR(20) PRIMARY KEY,
  3      CUSTOMER_STREET VARCHAR(15),
  4      CUSTOMER_CITY VARCHAR(10)
  5  );

Table created.

SQL>
SQL> CREATE TABLE LOAN (
  2      LOAN_NUMBER NUMBER(10) PRIMARY KEY,
  3      BRANCH_NAME VARCHAR(15),
  4      AMOUNT NUMBER(10),
  5      FOREIGN KEY (BRANCH_NAME) REFERENCES BRANCH(BRANCH_NAME)
  6  );

Table created.

SQL>
SQL> CREATE TABLE DEPOSITOR (
  2      CUSTOMER_NAME VARCHAR(20),
  3      ACCOUNT_NUMBER NUMBER(10),
  4      PRIMARY KEY (CUSTOMER_NAME, ACCOUNT_NUMBER),
  5      FOREIGN KEY (CUSTOMER_NAME) REFERENCES CUSTOMER(CUSTOMER_NAME),
  6      FOREIGN KEY (ACCOUNT_NUMBER) REFERENCES ACCOUNT(ACCOUNT_NUMBER)
  7  );

Table created.

SQL>
SQL> CREATE TABLE BORROWER (
  2      CUSTOMER_NAME VARCHAR(20),
  3      LOAN_NUMBER NUMBER(10),
  4      PRIMARY KEY (CUSTOMER_NAME, LOAN_NUMBER),
  5      FOREIGN KEY (CUSTOMER_NAME) REFERENCES CUSTOMER(CUSTOMER_NAME),
  6      FOREIGN KEY (LOAN_NUMBER) REFERENCES LOAN(LOAN_NUMBER)
  7  );

Table created.

SQL>
SQL> INSERT INTO BRANCH VALUES ('Brooklyn', 'New York', 2500000);

1 row created.

SQL> INSERT INTO BRANCH VALUES ('Perryridge', 'Horseneck', 1700000);

1 row created.

SQL>
SQL> INSERT INTO ACCOUNT VALUES (101, 'Brooklyn', 500);

1 row created.

SQL> INSERT INTO ACCOUNT VALUES (102, 'Perryridge', 400);

1 row created.

SQL>
SQL> INSERT INTO CUSTOMER VALUES ('Jones', 'Main', 'Harrison');

1 row created.

SQL> INSERT INTO CUSTOMER VALUES ('Smith', 'North', 'Rye');

1 row created.

SQL>
SQL> INSERT INTO LOAN VALUES (201, 'Brooklyn', 2000);

1 row created.

SQL> INSERT INTO LOAN VALUES (202, 'Perryridge', 1500);

1 row created.

SQL>
SQL> INSERT INTO DEPOSITOR VALUES ('Jones', 101);

1 row created.

SQL> INSERT INTO BORROWER VALUES ('Smith', 201);

1 row created.

SQL>
SQL> SELECT CUSTOMER_NAME, ACCOUNT_NUMBER FROM DEPOSITOR;

CUSTOMER_NAME        ACCOUNT_NUMBER
-------------------- --------------
Jones                           101

SQL>
SQL> SELECT BRANCH_NAME, ASSETS FROM BRANCH;

BRANCH_NAME         ASSETS
--------------- ----------
Brooklyn           2500000
Perryridge         1700000

SQL>
SQL> SELECT * FROM ACCOUNT WHERE BRANCH_NAME = 'Brooklyn';

ACCOUNT_NUMBER BRANCH_NAME        BALANCE
-------------- --------------- ----------
           101 Brooklyn               500

SQL>
SQL> SELECT * FROM LOAN WHERE AMOUNT > 1000;

LOAN_NUMBER BRANCH_NAME         AMOUNT
----------- --------------- ----------
        201 Brooklyn              2000
        202 Perryridge            1500

SQL>
SQL> UPDATE BRANCH SET ASSETS = 340000000 WHERE BRANCH_NAME = 'Perryridge';

1 row updated.

SQL>
SQL> SELECT * FROM BRANCH;

BRANCH_NAME     BRANCH_CITY              ASSETS
--------------- -------------------- ----------
Brooklyn        New York                2500000
Perryridge      Horseneck             340000000

SQL>