CREATE TABLE EMPLOYEE (
    EMP_NO NUMBER(5) PRIMARY KEY,
    EMP_NAME VARCHAR2(30),
    EMP_ADDRESS VARCHAR2(50)
);

INSERT INTO EMPLOYEE VALUES (101, 'Amit Shah', 'MANIPAL');
INSERT INTO EMPLOYEE VALUES (102, 'Sara Khan', 'MANGALORE');
INSERT INTO EMPLOYEE VALUES (103, 'John Doe', 'MANIPAL');
INSERT INTO EMPLOYEE VALUES (104, 'Anita Rao', 'MANGALORE');
INSERT INTO EMPLOYEE VALUES (105, 'Rahul V', 'BANGALORE');

SELECT EMP_NAME FROM EMPLOYEE;

SELECT * FROM EMPLOYEE WHERE EMP_ADDRESS = 'MANIPAL';

ALTER TABLE EMPLOYEE ADD SALARY NUMBER(10, 2);

UPDATE EMPLOYEE SET SALARY = 50000 WHERE EMP_NO = 101;
UPDATE EMPLOYEE SET SALARY = 45000 WHERE EMP_NO = 102;
UPDATE EMPLOYEE SET SALARY = 55000 WHERE EMP_NO = 103;
UPDATE EMPLOYEE SET SALARY = 48000 WHERE EMP_NO = 104;
UPDATE EMPLOYEE SET SALARY = 60000 WHERE EMP_NO = 105;

DESCRIBE EMPLOYEE;

DELETE FROM EMPLOYEE WHERE EMP_ADDRESS = 'MANGALORE';

RENAME EMPLOYEE TO EMPLOYEE1;

DROP TABLE EMPLOYEE1;